-- RELATIONAL DATABASE MANAGEMENT SYSTEM (RDMS)
CREATE DATABASE IF NOT EXISTS  RDMS;

-- USE DATABASE
USE RDMS;

-- CREATE TABLE
CREATE TABLE IF NOT EXISTS studentDetails (
rollNumber	int	not null unique,
registrationId	varchar(11) not null unique,
firstname varchar(300),
surname varchar(300),
fathersName varchar(20),
mothersName varchar(20),
email nvarchar(320) not null unique,
isAadhar boolean,
aadhar varchar(300) not null unique,
isPAN boolean,
PAN	varchar(300) unique,
isPassport boolean,
passport varchar(300) unique,
isIndian boolean,
nationality varchar(20),
mobileNumber varchar(300) not null unique,
permanantAddress varchar(500),
residentialAddress varchar(500),
tenthCGPA float,
twelthCGPA float,
tenthGrade float,
twelthGrade float,
firstSemCGPA float,
secondSemCGPA float,
thirdSemCGPA float,
fourthSemCGPA float,
fifthSemCGPA float,
sixthSemCGPA float,
seventhSemCGPA float,
eightthSemGCPA float,
isDiploma boolean,
diplomaMarks float,
isBacklog boolean,
numberOfBacklogs int,
activeBacklog int,
PassiveBacklog int,
isYD boolean,
YDYears int,
isEducationGap boolean,
educationGapYears int,
isPICTStudent boolean,
currentBatch int
);
